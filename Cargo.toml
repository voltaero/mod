[package]
name = "engine_core"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]
[dependencies]
enginelib = { git = "https://github.com/voltaero/engine.git" }
serde = { version = "1.0.228", features = ["derive"] }
toml = "1.0.6+spec-1.1.0"

[metadata.rustforge]
